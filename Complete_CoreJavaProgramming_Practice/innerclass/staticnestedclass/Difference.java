package com.practice.innerclass.staticnestedclass;

//outerClass
public class Difference {
	/*
	 * 	Normal/regular Inner 			             	vs 				Static Nested Inner class
	 * 
	 * without existing outer class object				without existing outer class object there may a chance of existing
	 * there is no chance of existing inner 			static nested class object hence static nested nested isnt strongly associated
	 * class object
	 * 
	 * in normal/regular inner class					       in static nested classes we can declare static member.
	 * we cant declared static members
	 * 
	 * we cant declare main method, so we can			we can declare main(), and hence we can invoke nested class
	 * cant invoke main() directly from cmd				directly from cmd
	 * 
	 * we can access both static and non-static			we can access only static members of outer class
	 * member if outer class directly.
	 */
}